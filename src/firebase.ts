import { initializeApp } from "firebase/app"; // initializes FireBase App object (like a container)
import { getFirestore, collection, getDocs } from "firebase/firestore"; // initializes access to firebase firestore...???

const firebaseConfig = {
  // AAAHHHHH AHHHHHHH OHHH GOD PLEASE WHYY WHHYYYYYY
};

const app = initializeApp(firebaseConfig);
const db = getFirestore(app);